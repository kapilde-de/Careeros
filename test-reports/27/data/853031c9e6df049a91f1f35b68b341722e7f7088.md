# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: navigation.spec.js >> Navigation >> homepage loads with correct title and hero text
- Location: tests/navigation.spec.js:19:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText('Get the interview.')
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for getByText('Get the interview.')

```

```yaml
- navigation:
  - text: 🎯 CareerOS
  - button "📝 Builder"
  - button "🎨 Templates"
  - button "🔍 Jobs"
  - button "🎤 Interview"
  - button "📊 Dashboard"
  - button "💳 Plans"
  - button "🤖 Agent"
  - button "Sign in"
  - button "Get started free"
- text: AI-Powered Career Platform Get the interview. Not the rejection.
- paragraph: CareerOS tailors your CV to every job description, scores your ATS odds, and arms you with a salary negotiation script — powered by AI.
- button "Start for free — no card needed →"
- button "I have an account"
- text: ✓ Free forever plan ✓ No credit card required ✓ Instant access 3× more interview callbacks 95% ATS pass rate 50K+ resumes optimized 160+ job sites scanned
- heading "Everything you need to land the job" [level=2]
- paragraph: Sign up free and get instant access to every feature.
- text: 🎯 Beats the ATS AI rewrites your CV to match every keyword the ATS scans for. Unlock free → ⚠️ Rejection Risk Score Know exactly why you'll get rejected — and how to fix it before you apply. Unlock free → 💰 Salary Negotiation Word-for-word script tailored to your role and market rate. Unlock free → 🤖 26 Premium Templates Stunning ATS-safe designs used by candidates landing roles at top companies. Unlock free → 📊 Interview Probability See your real odds of making it past the screen before you click send. Unlock free → 🔍 Job Agent Autonomous agent scans 160+ sites nightly and queues your best matches. Unlock free → Ready to stop getting rejected? Join thousands of candidates who landed their next role with CareerOS.
- button "Create your free account →"
```

# Test source

```ts
  1  | ﻿// @ts-check
  2  | import { test, expect } from '@playwright/test';
  3  | 
  4  | async function dismissModals(page) {
  5  |   try {
  6  |     await page.getByRole('button', { name: /essential only/i }).click({ timeout: 3000 });
  7  |   } catch {}
  8  |   try {
  9  |     await page.locator('button').filter({ hasText: /^[×✕x✖]$/ }).first().click({ timeout: 3000 });
  10 |   } catch {}
  11 |   try {
  12 |     const dialog = page.locator('[role="dialog"], [class*="modal"], [class*="overlay"]').first();
  13 |     if (await dialog.isVisible({ timeout: 1000 })) await page.keyboard.press('Escape');
  14 |   } catch {}
  15 | }
  16 | 
  17 | test.describe('Navigation', () => {
  18 | 
  19 |   test('homepage loads with correct title and hero text', async ({ page }) => {
  20 |     await page.goto('/');
  21 |     await dismissModals(page);
  22 |     await expect(page).toHaveTitle(/CareerOS/);
> 23 |     await expect(page.getByText('Get the interview.')).toBeVisible();
     |                                                        ^ Error: expect(locator).toBeVisible() failed
  24 |     await expect(page.getByText('Not the rejection.')).toBeVisible();
  25 |   });
  26 | 
  27 |   test('top nav shows all tabs', async ({ page }) => {
  28 |     await page.goto('/');
  29 |     for (const tab of ['Builder', 'Templates', 'Jobs', 'Interview', 'Dashboard', 'Plans']) {
  30 |       await expect(page.getByRole('button', { name: new RegExp(tab, 'i') })).toBeVisible();
  31 |     }
  32 |     await expect(page.getByText(/Agent/i).first()).toBeVisible();
  33 |   });
  34 | 
  35 |   test('hero feature tiles are all visible', async ({ page }) => {
  36 |     await page.goto('/');
  37 |     await dismissModals(page);
  38 |     await expect(page.getByText('Beats the ATS filter')).toBeVisible();
  39 |     await expect(page.getByText('Flags your rejection risk')).toBeVisible();
  40 |     await expect(page.getByText('Negotiate your best salary')).toBeVisible();
  41 |     await expect(page.getByText('Tailored to every job description')).toBeVisible();
  42 |   });
  43 | 
  44 |   test('clicking Plans tab shows pricing tiers', async ({ page }) => {
  45 |     await page.goto('/');
  46 |     await dismissModals(page);
  47 |     await page.getByRole('button', { name: /Plans/i }).click();
  48 |     await expect(page.getByText(/Free|Pro|Enterprise/i).first()).toBeVisible();
  49 |   });
  50 | 
  51 |   test('clicking Agent tab shows agent section', async ({ page }) => {
  52 |     await page.goto('/');
  53 |     await dismissModals(page);
  54 |     await page.getByRole('button', { name: /Agent/i }).click();
  55 |     await expect(page.getByText(/agent|autonomous|job hunter|scan/i).first()).toBeVisible();
  56 |   });
  57 | 
  58 | });
```